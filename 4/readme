Дерево — это иерархическая структура данных, где каждый элемент (узел) имеет потомков, кроме корневого. 
Используется для представления иерархий (файловые системы, деревья поиска, DOM в HTML).
Граф — это набор узлов (вершин), соединённых рёбрами. Применяется для моделирования сетей, маршрутов, связей между объектами
