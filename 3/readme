Бинарная куча — структура данных на основе двоичного дерева, где каждый узел меньше (или больше) своих потомков. Используется для реализации приоритетных очередей и алгоритмов сортировки (например, heapsort).
Биномиальная куча — набор двоичных деревьев, объединённых так, что можно эффективно выполнять операции объединения и извлечения минимума. Применяется в алгоритмах с частыми объединениями куч.
Куча Фибоначчи — усовершенствованная структура для реализации приоритетных очередей с очень быстрой операцией объединения. Используется в алгоритмах поиска кратчайших путей (например, Дейкстры).
Хеш-таблица — структура данных, хранящая пары «ключ–значение» и обеспечивающая быстрый доступ к элементам по ключу. Применяется в базах данных, кэшах и индексировании.

