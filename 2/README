1. Мультисписок
Мультисписок (или связный список списков) — это структура данных, в которой каждый элемент может содержать не только значение, но и ссылку на другие списки. По сути, это иерархическая структура, позволяющая хранить сложные взаимосвязанные данные.
Назначение:
удобен для представления древовидных и вложенных структур;
используется в системах, где элементы связаны не линейно, а через несколько направлений (например, организация меню, иерархия файлов, графы).

2. Очередь (Queue)
Очередь — это линейная структура данных, работающая по принципу FIFO (First In — First Out, «первым вошёл — первым вышел»).
Новые элементы добавляются в конец очереди, а удаляются из начала.
Назначение:
используется для организации буферов ввода-вывода;
применяется при планировании задач и потоков;
используется в алгоритмах поиска в ширину (BFS).

3. Дек (Deque — двусторонняя очередь)
Дек (от англ. double-ended queue) — структура данных, в которой добавление и удаление элементов возможно как с начала, так и с конца.
Это универсальная форма очереди, совмещающая свойства стека и обычной очереди.
Назначение:
используется в алгоритмах, где важна работа с обеих сторон;
применяется в реализациях буферов и кэшах;
удобен для реализации обходов в обе стороны.

4. Приоритетная очередь (Priority Queue)
Приоритетная очередь — это структура данных, где каждый элемент имеет свой приоритет.
Удаляется или обрабатывается элемент с наивысшим приоритетом, а не просто первый по порядку.
Назначение:
используется в алгоритмах сортировки и оптимизации (например, алгоритм Дейкстры);
применяется в планировщиках задач, симуляциях, системах обработки событий.
